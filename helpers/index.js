const generateError = require("./generateError");
const auth = require("./auth");
const processImage = require("./processImage");
const sendMail = require("./sendMail");

module.exports = { generateError, auth, processImage, sendMail };
